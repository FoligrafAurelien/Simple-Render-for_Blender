# Simple-Render-for_Blender
SimpleRender splits a complex Blender scene into isolated render jobs — one per asset collection — and lets you either launch them locally in a visible terminal or export them as standalone .blend files for any render farm (Deadline, Tractor, Royal Render, etc.). The key concept is a two-tier collection system:
